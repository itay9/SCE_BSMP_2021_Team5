
pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'python:3.7'
                }
            }
            steps {
                sh 'python --version'
                sh 'python -m py_compile src/login/GUI/*.py'
            }
        }
    }
}
